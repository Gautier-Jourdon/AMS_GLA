# Postgres connection
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=postgres
PGDATABASE=postgres

# JWT secret for auth
JWT_SECRET=change_this_secret

# Optional: Supabase proxy settings (if using Supabase)
SUPABASE_URL=
SUPABASE_KEY=
