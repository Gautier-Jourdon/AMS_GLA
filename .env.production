# Configuration Production - AMS GLA
# Copiez ce contenu dans votre fichier .env

# Mode Production (désactivé DEV_AUTH)
DEV_AUTH=false

# PostgreSQL via Docker (Port correct: 54322)
PGHOST=localhost
PGPORT=54322
PGUSER=postgres
PGPASSWORD=postgres
PGDATABASE=postgres

# Supabase (détecté automatiquement)
SUPABASE_URL=http://localhost:55321
SUPABASE_KEY=your-anon-key-here

# JWT
JWT_SECRET=your-secret-key-here

# SMTP (Gmail - optionnel)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_SECURE=false
EMAIL_FROM="AMS GLA <no-reply@ams-gla.com>"
