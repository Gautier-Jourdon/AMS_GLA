apiVersion: batch/v1
kind: CronJob
metadata:
  name: collector-cron
spec:
  schedule: "*/1 * * * *"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: collector
            image: docker.io/gautierjourdon/collector:latest
            
            env:
            - name: ENABLE_COLLECTOR
              value: "true"
          restartPolicy: OnFailure
